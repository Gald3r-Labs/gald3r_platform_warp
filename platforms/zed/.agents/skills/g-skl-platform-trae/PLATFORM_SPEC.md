---
subsystem_memberships: [PLATFORM_INTEGRATION]
platform: trae
authoring_path: create
docs_url: https://www.trae.ai
docs_url_secondary: []
crawl_max_age_days: 14
vault_doc_path: research/platforms/trae/
last_doc_scan: never
reference: g-skl-platform-cursor
status: ❓
task: T516
---

# PLATFORM_SPEC.md — TRAE (ByteDance)

> **Authoring status**: `create` (STUB — generated by T516 to end the ghost-platform
> invisibility). This platform ships an overlay (`platforms/trae/`) and is registered in
> `PLATFORM_REGISTRY.yaml`, but its capabilities are **untested / no verified info** — every cell
> below is `❓`. Run `@g-platform-scan-docs trae` (crawl `https://www.trae.ai`) then
> `@g-platform-check trae` to populate this spec honestly.

**TRAE (ByteDance)** is registered in the gald3r platform roster but has not yet had a verified
capability scan. The six gald3r-relevant primitives (hooks, rules, skills, commands, MCP, and the
AI instruction file) are **all unknown** until a live docs crawl + install test is run. This stub
exists so the platform is visible to the monitor tooling and the roster-parity gate instead of being
silently dropped — it makes **no capability claims**.

---

## 1. Folder Hierarchy

**Status: ❓ untested — no verified info.** The platform's config-folder layout has not been
crawled/verified. Do not assume a `.trae/` tree exists until `@g-platform-scan-docs trae`
confirms it.

## 2. AI Instruction File

**Status: ❓ untested.** Whether TRAE (ByteDance) auto-reads `AGENTS.md` / `CLAUDE.md` or a native
instruction file is unverified.

## 3. Agents Support — ❓ untested

No verified info. Pending a live docs crawl.

## 4. Skills Support — ❓ untested

No verified info. Pending a live docs crawl.

## 5. Commands / Workflows — ❓ untested

No verified info. Pending a live docs crawl.

## 6. Hooks System — ❓ untested

No verified info. Pending a live docs crawl.

## 7. Rules / Memory — ❓ untested

No verified info. Pending a live docs crawl.

## 8. MCP Support — ❓ untested

No verified info. Pending a live docs crawl.

---

## Parity vs. Cursor Reference

Parity is **unknown** — this is a stub. Compare against `g-skl-platform-cursor/PLATFORM_SPEC.md`
only after a verified scan.

## Hook System

- **Type**: ❓ untested
- **Config file**: unknown
- **Events available**: unknown
- **Event payload format**: unknown
- **gald3r hook files**: portability of `g-hk-*.ps1` is unverified

## Atypical Handling

- None recorded — stub. Capture atypical behaviors after the first verified scan.

## gald3r Integration Notes

- This is a **stub** (T516). Do not assume any `.claude/` reuse, hook, rule, skill, command, agent,
  or MCP wiring works on TRAE (ByteDance) until verified.
- Run `@g-platform-scan-docs trae` (crawl `https://www.trae.ai`) then `@g-platform-check trae`.

---

## Capability Summary (copy into PLATFORM_STATUS.md row)

| Hooks | Rules | Skills | Commands | MCP | Docs Fresh |
|---|---|---|---|---|---|
| ❓ | ❓ | ❓ | ❓ | ❓ | ❓ |

Legend: ✅ verified working · ⚠️ partial / Cursor-generic · ❌ not supported · ❓ untested.

Overall platform `status: ❓` — **untested stub** (T516). No verified capability information; every
cell is `❓` by design. This is the honest state until a live docs crawl + install test is run.

---

## Verification Evidence

| Capability | How verified |
|---|---|
| (all) | **Not yet verified** — stub generated by T516. Run `@g-platform-scan-docs trae`. |
