"""gald3r_templates framework test harness package (custom_scripts/tests).

Restored / re-armed under T1532. Holds the L1 (and discoverable L2/L3) framework
tests for the template distribution pipeline. Pure-Python tests in this package
are importable as ``custom_scripts.tests.*`` when ``custom_scripts`` is a package;
they are otherwise discovered and run by ``run_l1_tests.py`` via the
``tests_manifest.psd1`` test-plan manifest.
"""
